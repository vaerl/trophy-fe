# Trophy-FE

Frontend for evaluating the [Klostertrophy](https://klostertrophy.de/).

## Running

1. install dependencies: `npm i`
2. create the dotenv-file, see [the example](example.env) for necessary values
3. run the app: `npm run dev`
